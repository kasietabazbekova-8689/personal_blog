server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    location = /health {
        access_log off;
        return 200 "healthy\n";
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
